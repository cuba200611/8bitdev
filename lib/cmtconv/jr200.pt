from    cmtconv.jr200  import *
import  pytest

LL  = -32768
L   = -32600
Z   = 0
H   = 32600
HH  = 32767

@pytest.mark.parametrize('input, expected', (
    ((),                                ()),
    ((L, L, H, L),                      (0, 0, 1, 0)),
))
def test_nothing(input, expected):
    assert expected == tuple(samples_to_levels(input))
